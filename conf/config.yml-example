# Change these when deploying to production!
SECRET_KEY: "xx"
ALLOWED_HOSTS:
    - '*'

STATIC_ROOT: "/path/to/collected_static"
MEDIA_ROOT: "/path/to/media_root"

DEBUG: true

DATABASES:
    default:
        ENGINE: 'django.db.backends.postgresql'
        NAME: 'documentstore'

OCRSDK_PASSWORD: ""
OCRSDK_APP_ID: ""
